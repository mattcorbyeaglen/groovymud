package domains.minnovar;

import org.groovymud.object.ObjectLocation;

import std.game.objects.exits.ExitImpl;
/** test to see how easy i can make creating an area **/

area{
	street(RoomImpl){
		description = "This is a street."
		exits = {
			north(ExitImpl){
				
			}
			south(ExitImpl){
				
			}
		}
	}
}